DISPLAY PROPERTY

display --> property specifies if/how 

POSITIONS

Relative --> positioned relative to where it normally is 
fixed --> positioned relative to the viewport
absolute --> positioned relative to the nearest ancestor
sticky
static

flexbox

7/5/26

-Type Conversion
-Constant(const)
-Conditional Statements
    -if
    -if else
    -nested

-Checked Property

10questions
5-radio
5-checkbox

ternary operator- shortcut to if() and else statements helps to assign a variable based on condition

Logical Operators - used to combine or manipulate boolean values(true/false)

&& - AND
|| - OR
! - NOT

strict equality Operators
    = assignment operator  (assign values)
    == comparison operator (compare if values are equal)
    === strict equality operator (compare if values and datatypes are equal)
    != inequality operator
    !== strict inequality operator


control statements
    for
syntax:
    for(start,end,++/--){

    }

    
Functions - a section of reusable code,
            declared only once,

            
create a function to combine firstname and lastname to return as a username
function --> capitalize the first word(output: Ganesh Kumar)
create a function that return the square root
create a function to findout the area of circle

variable scope - where a variable  is recognized and accessible(local and global)

Arrays- list of values that can store more than 1 value

18/5/26
spread operator :-  ...
    it allows an iteration such as array or string to expanded into separate elements

rest parameters - allow a function to work with a variable number of arguments bundling them into an array

19/5/26
callbacks - functions that is  passed as an arguments to another function(used to handle asynchronous operations)
asynchronous-random flow
asynchronous operations ---> reading the files,network requests,interacting with databases

define the callback function

21/5/26

forEach() - it is a method to iterate over the element of an array and apply a specified function(callback) to each element
syntax = array.forEach(callback)


22/5/26
Map() - it accepts the callback and applies the fuction to each element of an array then return a new array

filter() - creates a new array by filtering out an element

.reduce()

25/5/26

function declaration - define a reusable block of code that perform a specific task

function expression - a way to define function as values or variables

//callbacks in asynchronous functions
//HOC-Higher Order Functions
//closures
//Event Listeners

arrow functions - a concise way to write function expression good for simple function  that we are use only once

syntax --- (parameter)=>

Authentication and Authorization Module

OOPS - Object Oriented Programming Structures - collection of class,properties,methods and classes

Object - a collection of related properties and/or methods can represent real world objects(people, products, places)

object={key: value,function()}

class{ properties, methods,objects}

THIS = reference to the object where this is used (the object depends on the immediate context)

person.name=this.name;

constructor = special method for defining the properties and methods of objects

classes -(ES6 feature)- provides a more
structure and cleaner way to work with
objects conapred to traditional construtor
functions.

class -static,super

Static Keyword = Keyword that defines properties or methods that belong only to a class itself rather than the object creted from that class (class owns anything static, not the object)

object,class,container,this keyword

03/06

destructuring - extract values from arrays and objects then assign them to variables in a convenient way

[] = to perform array destructuring
{} == to perform object destructuring


nested objects - objects inside another object allow us to represent more complex data structures child object is enclosed with a parent object

04/06

Array of objects 

Closures - a function defined inside the another function, the inner function has access to the variables and scope of the outer function 

allow for private variables and state maintenace 

used frequntly in js framework : react, vue, angular 

08/06

setTimeout() ---> function in javascript tha allaows us to shedule the executuion of a function after an amount of the time 


setTimeout(callback, delay)

Synchronous - Execute line by line consecutively in a sequential manner 

- code that waits for an operation to complete

Asynchronous - Allow multiple operations to be performed without waiting.

- does not block the execution flow and allows the progran to continue 

I/O operation, network requests, fetching data 

modified with: Callback, Promises, Async/wait

modified with: Callback, Promises, Async/wait

Error Handling - An object that is created to represent a problem that occurs often with user input or establishing a connection

try { } 

catch { }

finally { }

runtime time error

compile time error

16/06

callback hell - situation in javascript where callbacks nested within aother callbacks to the degree where the code is difficult to read

this is old pattern to handle asynchronous function

use promise+ async/await to avoid callback Hello

promises,Async/Await, json files, API data fetching

17/06

Promises - an object that manages asynchronous operations wrap a promise object around (asynchronous code)

"i promise to return a value"
pending -> resolved or rejected

new Promise((resolve,reject)=>(asynchronous code))

examples - fetching the file, DB connection, read and write the files

24/06

API Fetching - fetch() - used fro making HTTP reauests to fetch resources like JSON,images,files 

simplifying asynchronous data fetching in javascript and used for interacting with APIs to retrieve and send data asynchronous over the web

syntax - fetch(url, {options});

26/06

BOOTSTRAP

container - view for mobile, tablet, desktop
containers - 2 types => fixed width container and full width container

typography

text colours

grid system